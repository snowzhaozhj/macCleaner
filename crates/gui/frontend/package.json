{
  "name": "maccleaner-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "test": "vitest run",
    "e2e:install": "playwright install chromium",
    "e2e": "playwright test",
    "selftest": "pnpm check && pnpm build && pnpm test && pnpm e2e:install && pnpm e2e"
  },
  "dependencies": {
    "@tauri-apps/api": "^2"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@sveltejs/vite-plugin-svelte": "^5",
    "@tsconfig/svelte": "^5",
    "@types/node": "^26.1.0",
    "svelte": "^5",
    "svelte-check": "^4",
    "typescript": "^5",
    "vite": "^6",
    "vitest": "^4.1.10"
  }
}

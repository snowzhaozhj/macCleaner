{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口能力：核心命令 + 用 opener 跳转系统设置（FDA 引导）、dialog 原生目录选择（Purge 目标目录）。",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:default"
  ]
}
